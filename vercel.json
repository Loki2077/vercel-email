{
  "version": 2,
  "builds": [
    { "src": "api/**/*.js", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/$1" }
  ],
  "env": {}
  // 注意：请在Vercel平台上直接设置MAIL_USER和MAIL_PASS环境变量
}