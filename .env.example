# 邮箱配置
MAIL_USER=your_email@qq.com #发件邮箱
MAIL_PASS=your_email_password_or_authorization_code #发件邮箱smtp授权码
MAIL_HOST=smtp.qq.com #发件邮箱smtp服务器
MAIL_PORT=465 #发件邮箱smtp端口
MAIL_SECURE=true #是否使用ssl

# 环境配置
# NODE_ENV=development # 或 production （废弃）

# 安全限制配置
# 速率限制：每个IP在指定时间窗口内允许的最大请求次数
RATE_LIMIT_MAX_REQUESTS=10
# 速率限制时间窗口（毫秒），默认3600000（1小时）
RATE_LIMIT_WINDOW=3600000

# CORS配置：允许访问的域名列表，多个域名用逗号分隔
ALLOWED_ORIGINS=http://localhost:3000,https://your-production-domain.com

# 请求体大小限制（字节），默认1MB
MAX_REQUEST_SIZE=1048576

# 邮件内容大小限制（字节），默认100KB
MAX_EMAIL_CONTENT_SIZE=102400